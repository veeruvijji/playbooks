---
- name: Create users on all servers
  hosts: all
  tasks:
    - name: Create users
      user:
        name: admin
        password: admin
        home: /tmp/admin
        uid: 9090
        shell: /bin/bash
        comment: "A normal user"

...
